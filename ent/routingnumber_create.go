// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/robinhuiser/fca-emu/ent/routingnumber"
)

// RoutingNumberCreate is the builder for creating a RoutingNumber entity.
type RoutingNumberCreate struct {
	config
	mutation *RoutingNumberMutation
	hooks    []Hook
}

// SetNumber sets the "number" field.
func (rnc *RoutingNumberCreate) SetNumber(s string) *RoutingNumberCreate {
	rnc.mutation.SetNumber(s)
	return rnc
}

// SetType sets the "type" field.
func (rnc *RoutingNumberCreate) SetType(r routingnumber.Type) *RoutingNumberCreate {
	rnc.mutation.SetType(r)
	return rnc
}

// Mutation returns the RoutingNumberMutation object of the builder.
func (rnc *RoutingNumberCreate) Mutation() *RoutingNumberMutation {
	return rnc.mutation
}

// Save creates the RoutingNumber in the database.
func (rnc *RoutingNumberCreate) Save(ctx context.Context) (*RoutingNumber, error) {
	var (
		err  error
		node *RoutingNumber
	)
	if len(rnc.hooks) == 0 {
		if err = rnc.check(); err != nil {
			return nil, err
		}
		node, err = rnc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*RoutingNumberMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = rnc.check(); err != nil {
				return nil, err
			}
			rnc.mutation = mutation
			node, err = rnc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(rnc.hooks) - 1; i >= 0; i-- {
			mut = rnc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, rnc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (rnc *RoutingNumberCreate) SaveX(ctx context.Context) *RoutingNumber {
	v, err := rnc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// check runs all checks and user-defined validators on the builder.
func (rnc *RoutingNumberCreate) check() error {
	if _, ok := rnc.mutation.Number(); !ok {
		return &ValidationError{Name: "number", err: errors.New("ent: missing required field \"number\"")}
	}
	if _, ok := rnc.mutation.GetType(); !ok {
		return &ValidationError{Name: "type", err: errors.New("ent: missing required field \"type\"")}
	}
	if v, ok := rnc.mutation.GetType(); ok {
		if err := routingnumber.TypeValidator(v); err != nil {
			return &ValidationError{Name: "type", err: fmt.Errorf("ent: validator failed for field \"type\": %w", err)}
		}
	}
	return nil
}

func (rnc *RoutingNumberCreate) sqlSave(ctx context.Context) (*RoutingNumber, error) {
	_node, _spec := rnc.createSpec()
	if err := sqlgraph.CreateNode(ctx, rnc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (rnc *RoutingNumberCreate) createSpec() (*RoutingNumber, *sqlgraph.CreateSpec) {
	var (
		_node = &RoutingNumber{config: rnc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: routingnumber.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: routingnumber.FieldID,
			},
		}
	)
	if value, ok := rnc.mutation.Number(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: routingnumber.FieldNumber,
		})
		_node.Number = value
	}
	if value, ok := rnc.mutation.GetType(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeEnum,
			Value:  value,
			Column: routingnumber.FieldType,
		})
		_node.Type = value
	}
	return _node, _spec
}

// RoutingNumberCreateBulk is the builder for creating many RoutingNumber entities in bulk.
type RoutingNumberCreateBulk struct {
	config
	builders []*RoutingNumberCreate
}

// Save creates the RoutingNumber entities in the database.
func (rncb *RoutingNumberCreateBulk) Save(ctx context.Context) ([]*RoutingNumber, error) {
	specs := make([]*sqlgraph.CreateSpec, len(rncb.builders))
	nodes := make([]*RoutingNumber, len(rncb.builders))
	mutators := make([]Mutator, len(rncb.builders))
	for i := range rncb.builders {
		func(i int, root context.Context) {
			builder := rncb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*RoutingNumberMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, rncb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, rncb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, rncb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (rncb *RoutingNumberCreateBulk) SaveX(ctx context.Context) []*RoutingNumber {
	v, err := rncb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
